<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation">

    <activity
        android:id="@+id/mainActivity"
        android:name="ucm.tfg.pccomponentes.list.MainActivity"
        android:label="activity_list"
        tools:layout="@layout/activity_list" />
    <activity
        android:id="@+id/seguidosView"
        android:name="ucm.tfg.pccomponentes.list.SeguidosView"
        android:label="activity_seguidos_view"
        tools:layout="@layout/activity_seguidos_view" />
    <activity
        android:id="@+id/profile"
        android:name="ucm.tfg.pccomponentes.main.Profile"
        android:label="activity_profile"
        tools:layout="@layout/activity_profile" />
</navigation>